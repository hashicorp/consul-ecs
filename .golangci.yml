# Copyright IBM Corp. 2021, 2025
# SPDX-License-Identifier: MPL-2.0

version: "2"

linters:
  disable-all: true
  enable:
    - govet
    - unconvert
    - staticcheck
    - ineffassign
    - unparam

formatters:
  enable:
    - gofmt
    - goimports

run:
  concurrency: 2
  timeout: 1m
