version: 2.1
executors:
  go:
    docker:
      - image: docker.mirror.hashicorp.services/cimg/go:1.17

jobs:
  disabled_job:
    executor: go
    steps:
      - run: echo 'disabled'

workflows:
  disabled:
    when: false
    jobs:
    - disabled_job
